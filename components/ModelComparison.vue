<template>
  <div class="model-comparison-root">

    <!-- ══ LEFT: Modelo Leve ══ -->
    <div class="half half-left r1">
      <div class="desk-wrap">
        <div class="desk-surface desk-surface-small">
          <div class="doc doc-sm">
            <div class="doc-lbl doc-lbl-p">CLÁUSULA 4.2</div>
            <div class="doc-lines">
              <div class="dline p" /><div class="dline s" />
              <div class="dline" /><div class="dline xs" />
              <div class="dline" /><div class="dline s" />
            </div>
          </div>
          <div class="doc doc-sm">
            <div class="doc-lbl doc-lbl-p">PRAZO: 12M</div>
            <div class="doc-lines">
              <div class="dline" /><div class="dline p s" />
              <div class="dline" /><div class="dline" />
              <div class="dline xs" /><div class="dline p" />
            </div>
          </div>
          <div class="doc doc-sm">
            <div class="doc-lbl doc-lbl-p">R$ 150.000</div>
            <div class="doc-lines">
              <div class="dline s" /><div class="dline" />
              <div class="dline p" /><div class="dline xs" />
              <div class="dline" /><div class="dline s" />
            </div>
          </div>
        </div>
        <div class="legs legs-small">
          <div class="leg leg-p" /><div class="leg leg-p" />
        </div>
      </div>

      <div class="cap-wrap">
        <div class="cap-bar-track cap-bar-track-small">
          <div class="cap-bar-fill cap-bar-fill-p" />
        </div>
        <span class="cap-label">~30k tokens · 3 documentos</span>
      </div>
      <span class="token-badge">rápido · barato · triagem</span>
      
      <span class="caption caption-p">Modelo leve — mesa pequena</span>
    </div>

    <!-- ══ RIGHT: Modelo Topo ══ -->
    <div class="half r2">
      <div class="desk-wrap">
        <div class="desk-surface desk-surface-large">
          <div class="doc-rows">
            <!-- Row 1 -->
            <div class="doc-row">
              <div class="doc doc-lg">
                <div class="doc-lbl doc-lbl-h">CL. 1.1</div>
                <div class="doc-lines">
                  <div class="dline h" /><div class="dline s" />
                  <div class="dline" /><div class="dline xs" /><div class="dline" />
                </div>
              </div>
              <div class="doc doc-lg">
                <div class="doc-lbl doc-lbl-h">CL. 5.3</div>
                <div class="doc-lines">
                  <div class="dline" /><div class="dline h s" />
                  <div class="dline" /><div class="dline" /><div class="dline xs" />
                </div>
              </div>
              <div class="doc doc-lg">
                <div class="doc-lbl doc-lbl-w">COND. A</div>
                <div class="doc-lines">
                  <div class="dline h" /><div class="dline" />
                  <div class="dline s" /><div class="dline h" /><div class="dline" />
                </div>
              </div>
              <div class="doc doc-lg">
                <div class="doc-lbl doc-lbl-w">COND. B</div>
                <div class="doc-lines">
                  <div class="dline s" /><div class="dline" />
                  <div class="dline h" /><div class="dline xs" /><div class="dline" />
                </div>
              </div>
              <div class="doc doc-lg">
                <div class="doc-lbl doc-lbl-w">ANEXO II</div>
                <div class="doc-lines">
                  <div class="dline" /><div class="dline s" />
                  <div class="dline h xs" /><div class="dline" /><div class="dline s" />
                </div>
              </div>
              <div class="doc doc-lg doc-warn">
                <div class="doc-warn-icon">⚠</div>
                <div class="doc-lbl doc-lbl-h">24 MESES</div>
                <div class="doc-lines">
                  <div class="dline h" /><div class="dline h s" />
                  <div class="dline" /><div class="dline h xs" /><div class="dline h" />
                </div>
              </div>
            </div>
            <!-- Row 2 -->
            <div class="doc-row">
              <div class="doc doc-lg">
                <div class="doc-lbl doc-lbl-h">RESP. 1</div>
                <div class="doc-lines">
                  <div class="dline" /><div class="dline h s" />
                  <div class="dline" /><div class="dline xs" /><div class="dline h" />
                </div>
              </div>
              <div class="doc doc-lg doc-warn">
                <div class="doc-warn-icon">⚠</div>
                <div class="doc-lbl doc-lbl-h">CONTRAD.</div>
                <div class="doc-lines">
                  <div class="dline h" /><div class="dline h" />
                  <div class="dline s" /><div class="dline h xs" /><div class="dline h" />
                </div>
              </div>
              <div class="doc doc-lg">
                <div class="doc-lbl doc-lbl-h">SE →</div>
                <div class="doc-lines">
                  <div class="dline h s" /><div class="dline" />
                  <div class="dline h" /><div class="dline" /><div class="dline xs" />
                </div>
              </div>
              <div class="doc doc-lg">
                <div class="doc-lbl doc-lbl-h">ENTÃO →</div>
                <div class="doc-lines">
                  <div class="dline s" /><div class="dline h" />
                  <div class="dline" /><div class="dline h s" /><div class="dline" />
                </div>
              </div>
              <div class="doc doc-lg">
                <div class="doc-lbl doc-lbl-w">RESP. 4</div>
                <div class="doc-lines">
                  <div class="dline h" /><div class="dline s" />
                  <div class="dline" /><div class="dline h xs" /><div class="dline" />
                </div>
              </div>
              <div class="doc doc-lg doc-warn">
                <div class="doc-warn-icon">⚠</div>
                <div class="doc-lbl doc-lbl-h">RISCO:ALTO</div>
                <div class="doc-lines">
                  <div class="dline h" /><div class="dline h" />
                  <div class="dline h xs" /><div class="dline" /><div class="dline h s" />
                </div>
              </div>
            </div>
          </div>
        </div>
        <div class="legs legs-large">
          <div class="leg leg-h" /><div class="leg leg-h" />
        </div>
      </div>

      <div class="cap-wrap">
        <div class="cap-bar-track cap-bar-track-large">
          <div class="cap-bar-fill cap-bar-fill-h" />
        </div>
        <span class="cap-label">~200k tokens · 12+ documentos</span>
      </div>
      <span class="token-badge">robusto · analítico · contradições</span>

      <span class="caption caption-h">Modelo topo — mesa grande</span>
    </div>

  </div>
</template>

<script setup lang="ts">
// Standalone — inherits all --rf-* tokens from style.css
</script>

<style scoped>
/* ─── Root ─────────────────────────────────────────── */
.model-comparison-root {
  position: relative;
  width: 100%;
  font-family: "Space Grotesk", sans-serif;
  display: grid;
  grid-template-columns: 1fr 1fr;
  padding: 4rem 0 1.5rem;
}

/* ─── Halves ────────────────────────────────────────── */
.half {
  position: relative; z-index: 1;
  display: flex; flex-direction: column;
  align-items: center; justify-content: flex-end;
  padding: 1rem 2rem; gap: 0.9rem;
}
.half-left { border-right: 1px solid var(--rf-border-primary); }

/* ─── Caption ───────────────────────────────────────── */
.caption {
  font-family: "JetBrains Mono", monospace;
  font-size: 0.6rem; letter-spacing: 0.18em; font-weight: 700;
  text-transform: uppercase;
  margin-top: 0.5rem;
}
.caption-p { color: var(--rf-primary); }
.caption-h { color: var(--rf-highlight); }

/* ─── Desk wrapper ──────────────────────────────────── */
.desk-wrap { display: flex; flex-direction: column; align-items: center; width: 100%; }

/* ─── Desk surface ──────────────────────────────────── */
.desk-surface {
  display: flex; align-items: flex-end;
  gap: 6px; padding: 10px 14px 12px;
  position: relative; border-radius: 6px 6px 0 0;
}
.desk-surface::before {
  content: ""; position: absolute;
  top: 0; left: 0; right: 0; height: 1px;
  animation: scan 4s ease-in-out infinite; border-radius: 6px;
}
@keyframes scan {
  0%   { top: 0%;   opacity: 0; }
  10%  { opacity: 1; }
  90%  { opacity: 1; }
  100% { top: 100%; opacity: 0; }
}

.desk-surface-small {
  width: 220px;
  background: linear-gradient(180deg, color-mix(in srgb, var(--rf-primary) 7%, transparent) 0%, color-mix(in srgb, var(--rf-primary) 3%, transparent) 100%);
  border: 1px solid color-mix(in srgb, var(--rf-primary) 25%, transparent);
  border-bottom: 2px solid color-mix(in srgb, var(--rf-primary) 35%, transparent);
  box-shadow: 0 0 20px color-mix(in srgb, var(--rf-primary) 6%, transparent);
}
.desk-surface-small::before {
  background: linear-gradient(90deg, transparent, color-mix(in srgb, var(--rf-primary) 40%, transparent), transparent);
}

.desk-surface-large {
  width: 380px;
  background: linear-gradient(180deg, color-mix(in srgb, var(--rf-highlight) 6%, transparent) 0%, color-mix(in srgb, var(--rf-highlight) 2%, transparent) 100%);
  border: 1px solid color-mix(in srgb, var(--rf-highlight) 22%, transparent);
  border-bottom: 2px solid color-mix(in srgb, var(--rf-highlight) 32%, transparent);
  box-shadow: 0 0 20px color-mix(in srgb, var(--rf-highlight) 5%, transparent);
}
.desk-surface-large::before {
  background: linear-gradient(90deg, transparent, color-mix(in srgb, var(--rf-highlight) 40%, transparent), transparent);
}

/* ─── Legs ──────────────────────────────────────────── */
.legs { display: flex; justify-content: space-between; }
.legs-small { width: 190px; }
.legs-large { width: 350px; }
.leg { width: 8px; height: 22px; border-radius: 0 0 3px 3px; }
.leg-p {
  background: color-mix(in srgb, var(--rf-primary) 20%, transparent);
  border: 1px solid color-mix(in srgb, var(--rf-primary) 25%, transparent);
  border-top: none;
}
.leg-h {
  background: color-mix(in srgb, var(--rf-highlight) 15%, transparent);
  border: 1px solid color-mix(in srgb, var(--rf-highlight) 22%, transparent);
  border-top: none;
}

/* ─── Doc card ──────────────────────────────────────── */
.doc {
  display: flex; flex-direction: column; gap: 2px;
  border-radius: 3px; padding: 5px 5px 4px;
  flex-shrink: 0; position: relative;
}
.doc-sm {
  width: 52px; height: 62px;
  background: var(--rf-surface-secondary);
  border: 1px solid var(--rf-border-secondary);
}
.doc-lg {
  width: 46px; height: 56px;
  background: var(--rf-surface-primary);
  border: 1px solid var(--rf-border-primary);
}
.doc-warn {
  background: color-mix(in srgb, var(--rf-highlight) 6%, var(--rf-surface-primary)) !important;
  border-color: color-mix(in srgb, var(--rf-highlight) 30%, transparent) !important;
  box-shadow: 0 0 8px color-mix(in srgb, var(--rf-highlight) 8%, transparent);
}

/* ─── Doc label ─────────────────────────────────────── */
.doc-lbl {
  font-family: "JetBrains Mono", monospace;
  font-size: 0.38rem; font-weight: 700; letter-spacing: 0.03em;
  text-align: center; padding-bottom: 3px;
  border-bottom: 1px solid var(--rf-border-primary);
  margin-bottom: 1px;
  white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
}
.doc-lbl-p  { color: color-mix(in srgb, var(--rf-primary)   80%, transparent); }
.doc-lbl-h  { color: color-mix(in srgb, var(--rf-highlight) 80%, transparent); }
.doc-lbl-w  { color: var(--rf-text-muted); }

/* ─── Doc lines ─────────────────────────────────────── */
.doc-lines { display: flex; flex-direction: column; gap: 2.5px; padding-top: 1px; }
.dline { height: 1.5px; border-radius: 1px; background: var(--rf-border-secondary); }
.dline.p  { background: color-mix(in srgb, var(--rf-primary)   40%, transparent); }
.dline.h  { background: color-mix(in srgb, var(--rf-highlight) 40%, transparent); }
.dline.s  { width: 65%; }
.dline.xs { width: 45%; }

/* ─── Warn icon ─────────────────────────────────────── */
.doc-warn-icon {
  position: absolute; top: 2px; right: 3px;
  font-size: 0.45rem;
  color: var(--rf-highlight);
}

/* ─── Doc rows ──────────────────────────────────────── */
.doc-rows { display: flex; flex-direction: column; gap: 4px; width: 100%; }
.doc-row  { display: flex; gap: 5px; justify-content: center; }

/* ─── Capacity bar ──────────────────────────────────── */
.cap-wrap { display: flex; flex-direction: column; gap: 4px; align-items: center; width: 100%; }
.cap-bar-track {
  height: 4px; border-radius: 4px;
  background: var(--rf-border-secondary);
  position: relative; overflow: hidden;
}
.cap-bar-track-small { width: 220px; }
.cap-bar-track-large { width: 380px; }
.cap-bar-fill { height: 100%; border-radius: 4px; position: absolute; left: 0; top: 0; }
.cap-bar-fill-p {
  background: linear-gradient(90deg, color-mix(in srgb, var(--rf-primary) 40%, transparent), var(--rf-primary));
  width: 28%;
}
.cap-bar-fill-h {
  background: linear-gradient(90deg, color-mix(in srgb, var(--rf-highlight) 40%, transparent), var(--rf-highlight));
  width: 100%;
}
.cap-label {
  font-family: "JetBrains Mono", monospace;
  font-size: 0.5rem; color: var(--rf-text-muted); letter-spacing: 0.06em;
}

/* ─── Token badge ───────────────────────────────────── */
.token-badge {
  font-family: "JetBrains Mono", monospace;
  font-size: 0.5rem; font-weight: 600; letter-spacing: 0.06em;
  color: var(--rf-text-muted);
  background: var(--rf-surface-secondary);
  border: 1px solid var(--rf-border-primary);
  border-radius: 20px; padding: 3px 12px;
}

/* ─── Reveal animations ─────────────────────────────── */
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(20px); filter: blur(4px); }
  to   { opacity: 1; transform: translateY(0);    filter: blur(0); }
}
.r1 { opacity: 0; animation: fadeUp 0.9s var(--rf-ease, cubic-bezier(0.22,1,0.36,1)) 0.15s forwards; }
.r2 { opacity: 0; animation: fadeUp 0.9s var(--rf-ease, cubic-bezier(0.22,1,0.36,1)) 0.30s forwards; }
</style>